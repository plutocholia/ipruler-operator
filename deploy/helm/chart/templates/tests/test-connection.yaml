apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ipruler-controller.fullname" . }}-test-connection"
  labels:
    {{- include "ipruler-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ipruler-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
